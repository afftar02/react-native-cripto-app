import { StyleSheet, View, Text } from 'react-native';

export default function CryptInfoScreen() {
    return (
        <View style={styles.screenContainer}>
            <Text style={{color: "white"}}>More info screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    screenContainer: {
        
    },
});